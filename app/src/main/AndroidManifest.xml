<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seniorproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_leaf_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_leaf_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeniorProject">
        <activity android:name=".ScoreboardActivity"></activity>
        <activity android:name=".WateringActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".PlantActivity" />
        <activity android:name=".InfomationActivity" />
        <activity android:name=".EditPlantActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>